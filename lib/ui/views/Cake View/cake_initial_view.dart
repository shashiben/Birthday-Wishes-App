import 'package:flutter/material.dart';
import 'package:get/get.dart';

class CakeInitialView extends GetView {
  const CakeInitialView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
